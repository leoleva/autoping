<div class="bg-primary">
    <div class="container-lg">
        <div class="col-md-12 ">
            <nav class="navbar navbar-expand-lg navbar-dark bg-primary">
                <div class="container-fluid">
                    <a class="navbar-brand" href="{{ url('index') }}">AutoPing</a>
                    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarColor01" aria-controls="navbarColor01" aria-expanded="false" aria-label="Toggle navigation">
                        <span class="navbar-toggler-icon"></span>
                    </button>

                    <div class="collapse navbar-collapse" id="navbarColor01">
                        <ul class="navbar-nav me-auto">
                            <li class="nav-item">
                                <a class="nav-link {% if app.request.get('_route') == 'job_list' %}active{% endif %}" href="{{ url('job_list') }}">Skelbimai
                                </a>
                            </li>
                            {% if app.user is not null and app.user.userType.value == 'buyer' %}
                            <li class="nav-item">
                                <a class="nav-link {% if app.request.get('_route') == 'author_job_list' %}active{% endif %}" href="{{ url('author_job_list') }}">Mano skelbimai</a>
                            </li>
                            <li class="nav-item">
                                <a class="nav-link {% if app.request.get('_route') == 'add_job_view' %}active{% endif %}" href="{{ url('add_job_view') }}">Įdėti skelbimą</a>
                            </li>
                            {% elseif app.user is not null and app.user.userType.value == 'specialist' %}
                            <li class="nav-item">
                                <a class="nav-link {% if app.request.get('_route') == 'specialist_job_list' %}active{% endif %}" href="{{ url('specialist_job_list') }}">Mano užsakymai</a>
                            </li>
                            <li class="nav-item">
                                <a class="nav-link {% if app.request.get('_route') == 'specialist_offers' %}active{% endif %}" href="{{ url('specialist_offers') }}">Mano užsakymų pasiūlymai</a>
                            </li>
                            {% endif %}

                        </ul>
                        <ul class="navbar-nav d-flex">
                            {% if app.user == null %}
                                <li class="nav-item">
                                    <a class="nav-link" href="{{ url('register') }}">Registracija</a>
                                </li>
                                <li class="nav-item">
                                    <a class="nav-link" href="{{ url('app_login') }}">Prisijungimas</a>
                                </li>
                            {% else %}
                                <li class="nav-item dropdown ">
                                    <a class="nav-link dropdown-toggle" data-bs-toggle="dropdown" href="#" role="button" aria-haspopup="true" aria-expanded="false">Pasirinkimai</a>
                                    <div class="dropdown-menu">
                                        <a class="dropdown-item" href="{{ url('my_profile_update_view') }}">Profilio redagavimas</a>
                                        <div class="dropdown-divider"></div>
                                        <a class="dropdown-item" href="{{ url('app_logout') }}">Atsijungti</a>
                                    </div>
                                </li>
                            {% endif %}
                        </ul>
                    </div>
                </div>
            </nav>
        </div>
    </div>
</div>
